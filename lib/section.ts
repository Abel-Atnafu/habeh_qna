// Shared horizontal container + section vertical rhythm.
// Apply to every <section> wrapper so the home page stops "jumping edges".
export const SECTION_CONTAINER = 'max-w-6xl mx-auto px-4 sm:px-6 lg:px-8';
export const SECTION_PADDING = 'py-20 md:py-28';
export const SECTION = `${SECTION_CONTAINER} ${SECTION_PADDING}`;
